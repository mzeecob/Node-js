
// modules and require
// modules and patterns
module.exports.counter = function (arr) {
    return "this array have " + arr.length + " elements"
};

module.exports.adder = function (a,b){
    return `the sum is ${a+b}` // templating quotation
};

